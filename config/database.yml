# SQLite version 3.x
#   gem install sqlite3
development:  
  adapter: mysql2  
  database: findedm_development  
  user: root  
  password:  
  encoding: utf8

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
    adapter: mysql2  
    database: findedm_test
    user: root  
    password:  
    encoding: utf8
    
production:  
    adapter: mysql2  
    database: findedm_production
    user: root  
    password:  
    encoding: utf8
